name: medical_diagnostic_device
description: A Flutter application for medical diagnostic device.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  charts_flutter: ^0.12.0
  provider: ^6.0.5
  flutter_blue: ^0.8.0
  json_annotation: ^4.8.0
  flutter_secure_storage: ^5.0.2
  intl: ^0.18.0
  flutter_localizations:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
